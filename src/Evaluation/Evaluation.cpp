#include "Evaluation.h"
namespace Evaluation {
    inline float Evaluate(const int (&pieces)[64], const int (&colour)[64]) {
        for (int i = 0; i < 64; i++) {
            ;
        }
        return 0;
    }
}